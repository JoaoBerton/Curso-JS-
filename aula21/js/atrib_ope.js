/*
    Operadores de atribuição
        > Maior que;
        >= Maior ou igual a;
        < Menor que;
        <= Menor ou igual a;
        == Igualdade (Valor);
        === Igualdade estrita (Valor e Tipo);
        != Diferença (Valor);
        !== Diferença estrita (Valor e Tipo);
*/

const num1 = 78;
const num2 = 43;
const res = num1 !== num2;
console.log(res);