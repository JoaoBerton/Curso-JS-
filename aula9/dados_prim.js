                        //Tipos de dados primitivos
                        
// String, Number, Undefined, Null, Boolean.

const nome = 'João';    //String    -->São valores de texto, caracteres em geral entre:('',"",``).
const nome1 = "João";   //String    -->São valores de texto, caracteres em geral entre:('',"",``).
const nome2 = `João`;   //String    -->São valores de texto, caracteres em geral entre:('',"",``).

const num = 10; //Number -->São valores numéricos, isso abrange os valores como Float.    
const num1 = 10.90; //Number -->São valores numéricos, isso abrange os valores como Float.

let pessoaFisica;   //Undefined --> Não aponta para nenhum lugar da memória, mas definida pelo sistema.
const pessoaJuridica = null;    //Nulo --> Não aponta para nenhum lugar da memória.

const pessoaImp = true; //Boolean -->São valores lógicos: True ou False.
const pessoaImp = false;    //Boolean -->São valores lógicos: True ou False.